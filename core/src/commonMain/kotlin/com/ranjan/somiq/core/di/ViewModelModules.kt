package com.ranjan.somiq.core.di

import org.koin.dsl.module

// Core view models will be added here if needed
// SplashViewModel is in composeApp module since it depends on auth features
val coreViewModelModule = module {
    // No core view models yet
}