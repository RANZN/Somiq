ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ com.smartcents.server.ApplicationKt.module ]
    }
}

# This is the configuration for the local development environment.
db {
    driver = "org.h2.Driver"
    url = "jdbc:h2:file:./build/db"
    user = "root"
    password = ""
}